# 🐍 Python Fundamentals Refresher

![Python](https://img.shields.io/badge/Python-3.14-blue?logo=python&logoColor=white)
![Status](https://img.shields.io/badge/status-dusting%20off%20the%20rust-yellow)
![Made with](https://img.shields.io/badge/made%20with-%E2%98%95%20and%20muscle%20memory-brown)
![Vibes](https://img.shields.io/badge/vibes-immaculate-ff69b4)

> "It's not a bug, it's an undocumented feature." — me, probably, in this very notebook

Quick refresher notebook — knocking the rust off before jumping into anything heavier. Nothing new here, just `print()`, loops, and remembering that Python actually cares about indentation.

---

## 📋 What's inside

| Section | What it does | My commentary |
|---|---|---|
| 🌍 `print("hello world")` | The universal "does this thing even turn on" test | Every language's version of clearing your throat |
| 🔢 `int`, `float`, `str` | Type check with `type()` | Python's dynamic typing: "trust me bro" |
| 🔁 `while` loop | Counts 0 → 8, says bye | Loop, counter, done. Groundbreaking stuff |
| ➖ Line continuation `\` | Splits `Total = A + B + C` across lines | ⚠️ `A`, `B`, `C` don't actually exist anywhere. Left it in for the syntax, not the vibes — will `NameError` on you and that's on purpose |
| 📅 List literal | `Days = ["Mon", ...]` | The most exciting weekday list you'll see all day |
| 💬 Comments (`#`) | Notes to future me, who trusts past me way too much | |
| 🖨️ `print()` deep dive | `\n`, escaped quotes, `sep`, `end` | The Swiss Army knife function, revisited |

---

## 🎯 Why this exists

Because even people who *know* Python forget Python. This is me re-reading the instruction manual for a tool I use every day — like double-checking how to boil water.

## ⚙️ Environment

- Python 3 (tested on 3.14.7 — yes, the future is now)
- Jupyter Notebook / JupyterLab

## 🚀 Usage

```bash
jupyter notebook "25-09-2026_Python_Hello_world.ipynb"
```

Run it, laugh at the `NameError`, move on with your life.

---

⭐ If this refresher taught you nothing, that's the point — that means I still know what I'm doing.
